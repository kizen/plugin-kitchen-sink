- Adds the `getTickerPrice` automation step: fetches AAPL's current market price directly from
  Yahoo Finance's chart API (no auth) and writes it to the target output. Same shape as `dadJoke`.
- Adds the `stockPriceWriteback` action: fetches AAPL's current market price through the new
  `yahoo_finance` service and writes it back to the record (overwrite + append shapes),
  alongside the existing `dadJokeWriteback` demo.
